import React from 'react'

export default class Toolbar extends React.Component {
    constructor(...args) {
        super(...args)
    }

    render() {
        return (
            <nav>
                <h1>Mnemosyne</h1>
            </nav>
        )
    }
}
